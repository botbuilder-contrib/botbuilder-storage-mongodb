{
    "name": "testbot",
    "dependencies": {
        "botbuilder": "^4.0.6",
        "botbuilder-greeting": "^1.0.1",
        "mongodb": "^3.1.10",
        "restify": "^4.3.0"
    },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "tsc && node index.js",
    "watch": "tsc && node index.js"
  },
    "devDependencies": {
        "@types/node": "^6.0.52",
        "@types/restify": "^2.0.35",
        "dotenv": "^6.0.0"
    }
}
